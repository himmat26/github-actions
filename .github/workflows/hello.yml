# This is a workflow name
name: hello_workflow

on:
  push: 
    branches: main

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "Hello dosto"
      - name: say hello to users
        run: echo "Hello $USER "
      
  details:
    runs-on: ubuntu-latest
    steps:
      - name: check version of docker and python
        run: docker --version && python --version
